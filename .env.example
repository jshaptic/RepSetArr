# Copy to .env before `docker compose up`. These are referenced from config.yml
# as ${MDBLIST_API_KEY} and friends, so no secret is written into the config.
TZ=Europe/Riga
LOG_LEVEL=info

MDBLIST_API_KEY=
RADARR_API_KEY=
SONARR_API_KEY=
